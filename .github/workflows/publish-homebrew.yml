# User-owned custom publish job, invoked by the cargo-dist-generated
# release.yml (`publish-jobs = ["./publish-homebrew"]` in dist-workspace.toml).
# Unlike release.yml, `dist generate` never touches this file, so the
# GitHub App token-mint step below survives regeneration.
name: Publish homebrew formula

on:
  workflow_call:
    inputs:
      plan:
        required: true
        type: string
    # SEC-36 (TASK-1662): declare the one secret this workflow actually uses, so
    # the caller can pass it explicitly instead of forwarding every repo secret
    # with `secrets: inherit`. GH_APP_CLIENT_ID is a *variable*, not a secret —
    # `vars.*` is available to called workflows without being passed.
    secrets:
      GH_APP_PRIVATE_KEY:
        required: true

jobs:
  publish-homebrew-formula:
    runs-on: "ubuntu-22.04"
    env:
      PLAN: ${{ inputs.plan }}
      GITHUB_USER: "axo bot"
      GITHUB_EMAIL: "admin+bot@axo.dev"
    steps:
      - name: Mint homebrew-tap token (my-cloud-ci GitHub App)
        id: tap-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.GH_APP_CLIENT_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
          owner: rsvalerio
          repositories: homebrew-tap
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: true
          repository: "rsvalerio/homebrew-tap"
          token: ${{ steps.tap-token.outputs.token }}
      # So we have access to the formula
      - name: Fetch homebrew formulae
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          pattern: artifacts-*
          path: Formula/
          merge-multiple: true
      # This is extra complex because you can make your Formula name not match your app name
      # so we need to find releases with a *.rb file, and publish with that filename.
      - name: Commit formula files
        run: |
          git config --global user.name "${GITHUB_USER}"
          git config --global user.email "${GITHUB_EMAIL}"

          for release in $(echo "$PLAN" | jq --compact-output '.releases[] | select([.artifacts[] | endswith(".rb")] | any)'); do
            filename=$(echo "$release" | jq '.artifacts[] | select(endswith(".rb"))' --raw-output)
            name=$(echo "$filename" | sed "s/\.rb$//")
            version=$(echo "$release" | jq .app_version --raw-output)

            export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
            brew update
            # We avoid reformatting user-provided data such as the app description and homepage.
            brew style --except-cops FormulaAudit/Homepage,FormulaAudit/Desc,FormulaAuditStrict --fix "Formula/${filename}" || true

            git add "Formula/${filename}"
            git commit -m "${name} ${version}"
          done
          git push
