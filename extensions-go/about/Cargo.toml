[package]
name = "ops-about-go"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ops-core = { workspace = true }
ops-extension = { workspace = true }
ops-about = { workspace = true }
# ARCH-11 (TASK-1738): `linkme` has no textual use here — `impl_extension!`
# expands to `#[linkme::distributed_slice(...)]` and resolves the path in the
# calling crate, so this entry is load-bearing despite the empty grep.
linkme = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
ops-about = { workspace = true, features = ["test-support"] }
ops-extension = { workspace = true, features = ["test-support"] }
tempfile = { workspace = true }

[lints]
workspace = true
