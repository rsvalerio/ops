# Default commands for Java stack (Maven).

[commands.compile]
program = "./mvnw"
args = ["compile"]
help = "Compile sources"
category = "Build"

[commands.build]
program = "./mvnw"
args = ["package", "-DskipTests"]
help = "Build the project"
category = "Build"

[commands.test]
program = "./mvnw"
args = ["test"]
help = "Run all tests"
category = "Code Quality"

[commands.clean]
program = "./mvnw"
args = ["clean"]
help = "Clean build artifacts"
category = "Build"

# Baseline suggestion — uncomment and adjust to opt in (requires spotless plugin).
# [commands.fmt]
# program = "./mvnw"
# args = ["spotless:apply"]
# help = "Format all code (requires spotless plugin)"
# category = "Code Quality"

# Baseline suggestion — uncomment and adjust to opt in (requires spotless or checkstyle plugin).
# [commands.lint]
# program = "./mvnw"
# args = ["spotless:check"]
# help = "Lint sources (requires spotless or checkstyle plugin)"
# category = "Code Quality"

[commands.verify]
commands = ["compile"]
parallel = false
fail_fast = true
help = "Run compile"
category = "Code Quality"

[commands.qa]
commands = ["test"]
parallel = false
fail_fast = true
help = "Run test"
category = "Code Quality"
